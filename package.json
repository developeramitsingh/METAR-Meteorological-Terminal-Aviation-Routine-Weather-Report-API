{
  "name": "amit-Weather-api",
  "version": "1.0.0",
  "description": "a weather api with redis cache",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix taskassigner && npm run build --prefix taskassigner"
  },
  "author": "amit singh",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0",
    "redis": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
